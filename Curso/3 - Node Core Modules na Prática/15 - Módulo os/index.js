const os = require('os')

console.log(os.cpus()) // Dados dos processadores
console.log(os.freemem()) // Memória livre
console.log(os.homedir()) // Diretório raiz do sistema
console.log(os.type()) // Tipo do sistema operacional